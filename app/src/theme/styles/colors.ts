const primary = "#4CBFA6"; // green
const secondary = "#F69F83"; //orange

const bgDark = "#1b1b1b";
const paperDark = "#424242";

const bgLight = "#64d8cb";
const paperLight = "#fff";


export const colors = {
  palette: {
    default: "inherit",
    primary: primary,
    secondary: secondary,
    dark: bgDark,
    light: bgLight,
    paperDark: paperDark,
    paperLight: paperLight,
  },
};
