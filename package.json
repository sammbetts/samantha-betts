{
  "name": "samantha-betts",
  "version": "1.0.0",
  "description": "Personal Web Page",
  "homepage": "https://sammbetts.github.io/samantha-betts/",
  "scripts": {
    "setup": "cd app && yarn install && cd backend && yarn install",
    "frontend": "cd app && yarn start",
    "backend": "cd app/backend && yarn start",
    "start": "concurrently --kill-others-on-fail \"yarn run frontend\" \"yarn run backend\""
  },
  "license": "Unlicense",
  "private": true,
  "devDependencies": {
    "concurrently": "^4.0.1",
    "@types/jest": "^28.1.0",
    "prettier": "^2.7.1"
  },
  "engines": {
    "node": "16"
  }
}
